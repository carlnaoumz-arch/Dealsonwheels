'use client';
import {useState} from 'react';
import {ArrowLeft,ArrowRight} from 'lucide-react';
export default function Gallery({photos,title}:{photos:string[];title:string}){const [i,setI]=useState(0);return <section className="vehicle-gallery" aria-label={`${title} photo gallery`}><div className="gallery-main"><img src={photos[i]} alt={`${title}, photograph ${i+1}`} fetchPriority="high" width={1080} height={1200}/><div className="gallery-controls"><button aria-label="Previous photograph" onClick={()=>setI(n=>(n-1+photos.length)%photos.length)}><ArrowLeft size={21}/></button><span aria-live="polite">{String(i+1).padStart(2,'0')} / {String(photos.length).padStart(2,'0')}</span><button aria-label="Next photograph" onClick={()=>setI(n=>(n+1)%photos.length)}><ArrowRight size={21}/></button></div></div><div className="thumbnails">{photos.map((url,j)=><button key={url} aria-label={`View photograph ${j+1}`} aria-pressed={i===j} onClick={()=>setI(j)}><img src={url} alt="" loading="lazy" width={130} height={100}/></button>)}</div></section>}
